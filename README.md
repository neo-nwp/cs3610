To run the Task1, Enter the following command in the terminal::
python -m FactoryMethodZoo.clientZoo.py

To run the Task2, Enter the following command in the terminal::
python -m AbstractFactoryFood.clientFoodApp

To run the Task3, Enter the following command in the terminal::
python -m BuilderCustomer.clientCustomerApp.py





